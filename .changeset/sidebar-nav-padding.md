---
"iryx-ui": patch
---

`ISidebar`'s nav had `px-2 py-1`, so a link sat 8px from the side edges but only 4px from the top — the first link read as crowded against the header. It is now an even `p-2`.
