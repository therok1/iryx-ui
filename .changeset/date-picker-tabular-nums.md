---
"iryx-ui": patch
---

`IDatePicker` and `IDateRangePicker` now render calendar day numbers with `tabular-nums`. A proportional `1` is narrower than a `0`, so days sat off-centre in their pill and the week columns visibly wobbled down the month.
