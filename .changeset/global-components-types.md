---
"iryx-ui": minor
---

Declare every component on Vue's `GlobalComponents`, so globally registered `I`-prefixed components are type-checked in templates. `IryxUiGlobalComponents` is exported for projects that register under a different prefix.
