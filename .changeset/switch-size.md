---
"iryx-ui": minor
---

`ISwitch` now takes a `size` prop (`sm` / `md` / `lg`), matching `ICheckbox`. The track, thumb and travel scale together, so the thumb rests the same 2px inside the track edge at every size, and the small track is nudged to centre against a label first line.
